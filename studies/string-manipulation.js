// 1. String manipulation with operators
var a = 'hi';
var b = 'bye';
console.log(a+b); //prints => "hibye"


console.log((a) + (1)); // prints => "hi1"

/*
+,+=  concats strings
(x) + (#)  returns both combined together in a string

*/

//2. String manipulation with string methods
var string = 'hunter';
console.log(string.length); //prints => 6

/*
methods available to simple values because javascript treats them as objects
and objects have methods
can apply methods to strings now
-.length
-.indexOf
-.slice
-etc
*/